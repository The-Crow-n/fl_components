import 'package:flutter/material.dart';

class DebianScreen extends StatelessWidget{
  const DebianScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}