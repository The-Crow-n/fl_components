import 'package:flutter/material.dart';

class ArchScreen extends StatelessWidget{
  const ArchScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}